# Example Repository for bs-node-ext.

# Build

```
npm run build
```

## Usage

Build the project then run

```sh
curl localhost:60023
```

To see the output from the server.

# Watch

```
npm run watch
```

# Editor

If you use `vscode`, Press `Windows + Shift + B` it will build automatically
